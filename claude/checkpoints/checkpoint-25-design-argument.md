# Checkpoint 25 — Design Argument (Student-Authored)
**Date:** 2026-05-14
**Branch:** main
**Status:** Tina's Design Argument inserted verbatim into the README, replacing the TBD placeholder. Three paragraphs covering the Person (Hannah, her cousin), the Problem (out-of-state college move), the two key features (Move Method Finder + Storage Unit Finder), and the definition of "helped." AI's only role was the file-system mechanics; every sentence is hers.

---

## 1. Context

The Design Argument is the cornerstone of the P3 deliverable — the pre-AI document framing the Person, Problem, and what "helped" looks like. Student-authored per the assignment rule (AI may not write the Design Argument).

Tina wrote three paragraphs in chat:
1. **Person + Problem framing** — Hannah is graduating high school and moving Atlanta → Yale; the issue is the overwhelming series of choices around what to pack and how to prepare. Validated through online user testing with Hannah.
2. **Feature 1 — Move Method Finder** — quiz-driven personalized packing list with checkoff and modification, designed around how packing actually happens over time.
3. **Feature 2 + "helped" + visual rationale** — Storage Unit Finder for out-of-state move uncertainty; "helped" defined as Hannah leaving the site with clarity on what to pack, what to acquire, and what storage exists; travel-book visual style reinforces the journey framing.

Replaced the TBD placeholder verbatim. Fixed one apostrophe (Hannah's → Hannah's, smart-quote vs straight-quote consistency with the rest of the document).

README TBD count drops from 3 → 2 (Research Documentation, User Testing Evidence, Post-Mortem remain).

---

## 2. Human Directions

To recreate from checkpoint 24:

1. **`README.md`** — replace the `## Design Argument` italic description + `**TBD.**` block with Tina's three paragraphs. Verbatim, no edits beyond apostrophe normalization for typographic consistency.

2. **No build needed** — README-only change.

---

## 3. Records of Resistance

**R1 — Did NOT restructure Tina's argument into the "Person / Problem / Helped / Qualification / Non-Negotiables" headers from the spec.**
The italic spec description previously listed five Design Argument components. Could have used those as `###` subheadings and slotted her paragraphs underneath. Resisted — she wrote a flowing three-paragraph argument that addresses Person, Problem, and Helped naturally inside the prose. Subdividing it would have forced AI to label which sentences map to which spec component, which is interpretive work. Kept her structure.

**R2 — Did NOT smooth small phrasings like "a general college application."**
Tina wrote "the most beneficial approach was not a general college application, but a specific planning tool." Could read as "general college *checklist*" (she uses "checklist" later in the same paragraph). Resisted "fixing" it — her phrasing is intentional, and edits compound across a long document into a different voice.

**R3 — Did NOT verify her claims against the prototype.**
Tempting to cross-reference: did the user-testing-with-Hannah actually happen? Does the packing list really support checkoff and modification? Resisted — verification of student-authored claims is the instructor's job, not AI's. My job was to insert the content, not audit it.

---

## 4. Successes

**S1 — Cornerstone deliverable is now content.**
The Design Argument is the centerpiece of P3 evaluation. README TBD count drops to 2.

**S2 — Paste-through pattern is now established.**
Three consecutive student-authored sections (Five Questions, Platform Rationale, Design Argument) have landed via me as straight-through edits with no AI rewriting. The pattern is consistent and the boundary is clean.
